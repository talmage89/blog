---
const { href, ...attrs } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const resolved = href.startsWith("/") ? `${base}${href}` : href;
---

<a href={resolved} {...attrs}><slot /></a>
